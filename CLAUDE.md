# Dockit

## Agent skills

### Issue tracker

Issues and specs live as GitHub issues in this repo, accessed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
